pub const HELP_DISCOVERY_HINT: &str =
    "Use `swarm --help` for the model and `swarm <command> --help` for command-specific options.";

pub const PARENT_REPLY_HINT: &str =
    "Reply to your parent with `swarm send parent \"message\"`; harness stdout is process output, not a reply.";
